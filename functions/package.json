{
  "name": "stl-risk-monitor-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "uuid": "^11.0.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
